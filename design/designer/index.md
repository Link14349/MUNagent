# 会场设计器(Designer) 设计 — 索引

> 本目录是重构后的新设计文档起点. 旧 `docs/design/` 已整体废弃, 本目录当前只覆盖**会场设计器(场景创建 Agent)的前端界面**; 推演大厅、复盘页等其余前端与后端设计将在各自动工前另行成册.
>
> 顶层概念(设计八步流程、场景包结构、配置体系)以 [introduction.md](../../introduction.md) 为准; 本目录在其之下做界面级细化. 冲突时先改这里并回头修订 introduction.md.



## 场景包数据设计

场景包文件结构文件格式见./scenarios下面的参考.

然后场景包文件结构里面还有个文件夹是./chats/, 用来放在这个场景包中所有的与agent的对话记录. chats格式自行设计: (基本的要素: 用户发送的信息, agent回复的信息, agent编辑的文件，编辑内容, 调用的工具等等...)

这也就是说, agent对话和场景文件夹是绑定的

## UI 设计

类似cursor, 有两种模式，可以切换：

- 编辑模式: 左边栏显示文件树，主界面是一个编辑器，右侧边栏是agent会话(给一个小下拉按钮显示这个文件夹内所有的recent chat), 左边栏和右边栏可以选择是否隐藏; 
- 对话模式: 左边栏显示最近对话，主界面是与agent的会话界面，右边栏是文件树与文件预览. 左边栏和右边栏可以选择是否隐藏; 

## 细化文档

以上两节是总纲; 界面与数据的细化按下列文档执行, 与总纲冲突时以总纲为准并回改细化文档:

| 文档 | 内容 |
|---|---|
| [01-data-chats.md](01-data-chats.md) | 场景包结构(含 chats/ 与 .history/)、chat 的 JSONL 记录格式(消息/工具调用/文件编辑/usage)、编辑落盘与撤销语义、对话生命周期、版本快照的数据与恢复语义 |
| [02-ui-modes.md](02-ui-modes.md) | 编辑/对话双模式布局与切换、路由与首页入口、文件树/编辑器/预览的行为、人机写冲突处理、校验 chip 与导出、历史版本面板 |
| [03-agent-interaction.md](03-agent-interaction.md) | 对话面板(记录渲染/编辑卡/diff/撤销/思考块)、agent 任务与 SSE 流式协议、REST 契约(含快照接口)、前端代码组织、设计 Agent loop(原生 function calling + 流式三通道)与 LLM 层要求 |



