# AGENTS.md - MUNagent 项目级协作约定

本文件约束整个仓库.子目录中的 `AGENTS.md` 可以为该目录追加更具体的规则;用户的明确要求始终优先.

## 项目目标与当前边界

MUNagent 是使用 Python 开发的模拟联合国历史委员会推演 Agent.系统从场景包加载会议,角色和规则,为每名代表创建独立 Agent,由通用引擎推进,裁定并记录推演.

当前里程碑只实现单会场闭环.多会场,跨会场通信和分布式架构不属于当前范围,不要提前实现.

## 规则与文档优先级

1. 用户在当前任务中的明确要求;
2. 当前文件所在目录向上继承到根目录的 `AGENTS.md`;
3. `introduction.md` 的项目边界;
4. `docs/` 中对应的设计说明;
5. 代码和测试所体现的既有行为.

发现冲突时先判断是未同步文档还是设计变更.不要静默选择一方.修改公开结构或接口时,应在同一次改动中同步相应文档和示例.

## 目录职责

```text
introduction.md       项目概览与当前阶段
docs/                 项目设计与格式说明
src/                  通用 Python 引擎
scenario-template/    标准场景包兼示例;遵守其目录内 AGENTS.md
```

- 通用加载,动作校验,状态转换,调度,可见性,时钟,终局判定和存档能力放在 `src/`;
- 场景包只保存历史内容和声明式配置,不包含可执行 Python 机制;
- 不让场景数据反向决定通用引擎架构;
- 不为尚未进入当前里程碑的能力建立空洞抽象.

## 工程约定

- 以 Python 3.11+ 为目标,公开接口提供完整类型标注;
- 优先使用标准库;新增第三方运行时依赖前先征得用户同意;
- 不要添加无意义的运行时 `isinstance` / 类型守卫:参数类型由类型标注与静态检查保证;仅在解析外部无类型输入(如 YAML)或消除合法联合类型歧义时才做运行时类型分支;
- 状态转换应确定,可审计;相同输入必须产生相同输出;
- 可见性过滤必须由程序执行,不能仅靠提示词要求 Agent 忽略秘密;
- 错误信息应指出具体字段,对象和违反的不变量;
- 注释解释设计原因,不复述代码;用户可见文本和项目文档使用简体中文.

## 修改与验证

1. 修改前阅读任务涉及的代码,文档和适用的嵌套 `AGENTS.md`;
2. 保持改动聚焦,不顺手重构无关区域;
3. 新增或修改公开格式时同步 `docs/`;
4. 运行与风险相称的解析,类型,单元或集成验证;
5. 交付时说明已完成内容,验证结果和明确未实现项.

仓库可能处于重构中的脏工作区.未经用户明确要求,禁止恢复已删除文件,重置分支,清理未跟踪文件或覆盖无关的用户改动.

## 安全

- 不提交 API key,`.env`,本地配置,私密推演记录或包含角色秘密的调试提示词;
- LLM 测试必须 mock,不消耗真实 API;
- 不把密钥,内部服务地址或用户本机绝对路径写入可分享产物.
